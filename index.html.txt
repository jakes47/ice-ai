<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Liquidity Bot</title>
    <style>
        body {
            background-color: #0a0a0a;
            color: #fff;
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 20px;
        }
        .logo {
            max-width: 150px;
            margin-top: 20px;
        }
        .container {
            max-width: 800px;
            margin: auto;
            padding: 10px;
        }
        .code-box {
            background: #1e1e1e;
            padding: 15px;
            border-radius: 5px;
            text-align: left;
            overflow-x: auto;
            font-family: monospace;
            margin-top: 20px;
        }
        .live-status {
            margin-top: 20px;
            padding: 10px;
            border-radius: 5px;
            background: #222;
            display: inline-block;
        }
        .status-dot {
            height: 10px;
            width: 10px;
            background-color: red;
            border-radius: 50%;
            display: inline-block;
        }
        /* Ensure responsiveness */
        @media (max-width: 600px) {
            body {
                padding: 10px;
            }
            .logo {
                max-width: 120px;
            }
            .container {
                width: 100%;
                padding: 5px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <img src="logo.png" alt="Coin Logo" class="logo">
        <h1>AI-Powered Liquidity Bot</h1>
        <p>Optimized for Solana & real-time trading strategies.</p>

        <h2>Live Bot Status</h2>
        <div class="live-status">
            <span class="status-dot" id="botStatus"></span> <span id="statusText">Offline</span>
        </div>

        <h2>Sample Code Snippet</h2>
        <div class="code-box">
            <code>
                async function getLiquidity() {
                    let response = await fetch('https://api.solana.com');
                    let data = await response.json();
                    console.log(data);
                }
            </code>
        </div>
    </div>

    <script>
        // Simulating bot status update (this should be replaced with actual logic to check the bot status)
        async function checkBotStatus() {
            try {
                let response = await fetch('https://api.solana.com/status');  // Replace with actual status API
                let data = await response.json();
                if (data.status === "online") {
                    document.getElementById("botStatus").style.backgroundColor = "green";
                    document.getElementById("statusText").textContent = "Online";
                } else {
                    document.getElementById("botStatus").style.backgroundColor = "red";
                    document.getElementById("statusText").textContent = "Offline";
                }
            } catch (error) {
                document.getElementById("botStatus").style.backgroundColor = "red";
                document.getElementById("statusText").textContent = "Error connecting";
            }
        }
        
        // Check bot status after page load
        window.onload = checkBotStatus;
    </script>
</body>
</html>

      
        


